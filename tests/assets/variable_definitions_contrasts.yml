  variables:
    treatment:
      type: string
      description: treatment group with explicit ordering
      enum: ["Control", "Treated"]

  contrasts:
    - id: treatment_Treated_vs_Control
      formula: "~ treatment"
      make_contrasts_str: "treatmentTreated"